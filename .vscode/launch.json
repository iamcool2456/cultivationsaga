{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Cultivation Saga in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Dev Server (Start)"
    },
    {
      "name": "Open Cultivation Saga (no start)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
